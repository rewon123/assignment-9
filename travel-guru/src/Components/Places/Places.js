import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import './Places.css'
const Places = (props) => {
    const { img, id,handle} = props.place;
    return (
        <div>
            
        </div>
    );
};

export default Places;